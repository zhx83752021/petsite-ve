{
  "buildCommand": "cd frontend && npm ci && npm run vercel-build && cd ../backend && npm ci",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
