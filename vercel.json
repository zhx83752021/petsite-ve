{
  "buildCommand": "cd frontend && npm ci --include=dev && npm run vercel-build && cd ../backend && npm ci --include=dev",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
