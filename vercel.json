{
  "buildCommand": "npm install --prefix frontend && npm run vercel-build --prefix frontend && npm install --prefix backend",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
