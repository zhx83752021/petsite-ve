# 宠物网需求文档 - 后台管理系统

## 三、后台管理系统

### 3.1 系统架构

```
后台管理系统
├── 登录认证
├── 权限控制（RBAC）
├── 功能模块
│   ├── 系统管理
│   ├── 用户管理
│   ├── 商品管理
│   ├── 订单管理
│   ├── 社区管理
│   ├── 服务管理
│   ├── 内容管理
│   ├── 营销管理
│   └── 数据统计
└── 通用组件
    ├── 表格组件
    ├── 表单组件
    ├── 图表组件
    └── 富文本编辑器
```

### 3.2 系统管理

#### 3.2.1 管理员管理

**列表页：**

| 字段     | 类型     | 说明               |
| -------- | -------- | ------------------ |
| ID       | number   | 管理员 ID          |
| 账号     | string   | 登录账号           |
| 姓名     | string   | 真实姓名           |
| 角色     | string[] | 角色标签           |
| 状态     | enum     | 正常/禁用          |
| 最后登录 | datetime | 最后登录时间       |
| 创建时间 | datetime | 创建时间           |
| 操作     | actions  | 编辑/重置密码/删除 |

**表单字段：**

```typescript
interface AdminForm {
  username: string; // 账号（3-20字符）
  password: string; // 密码（首次创建必填）
  realName: string; // 真实姓名
  email: string; // 邮箱
  phone: string; // 手机号
  roleIds: string[]; // 角色ID数组
  status: "active" | "disabled"; // 状态
}
```

**权限要求：**

- 查看：`admin:view`
- 添加：`admin:create`
- 编辑：`admin:update`
- 删除：`admin:delete`
- 重置密码：`admin:resetPassword`

#### 3.2.2 角色管理

**预设角色：**

| 角色名     | 权限范围               | 说明         |
| ---------- | ---------------------- | ------------ |
| 超级管理员 | 全部权限               | 系统最高权限 |
| 运营人员   | 商品、订单、内容、营销 | 日常运营管理 |
| 客服人员   | 用户、订单、售后       | 客户服务支持 |
| 财务人员   | 订单（只读）、财务数据 | 财务数据查看 |

**权限树结构：**

```json
{
  "permissions": [
    {
      "module": "系统管理",
      "key": "system",
      "children": [
        {
          "name": "管理员管理",
          "key": "admin",
          "actions": ["view", "create", "update", "delete"]
        },
        {
          "name": "角色管理",
          "key": "role",
          "actions": ["view", "create", "update", "delete"]
        },
        { "name": "操作日志", "key": "log", "actions": ["view"] }
      ]
    },
    {
      "module": "商品管理",
      "key": "product",
      "children": [
        {
          "name": "商品列表",
          "key": "product",
          "actions": ["view", "create", "update", "delete"]
        },
        {
          "name": "商品分类",
          "key": "category",
          "actions": ["view", "create", "update", "delete"]
        },
        {
          "name": "品牌管理",
          "key": "brand",
          "actions": ["view", "create", "update", "delete"]
        }
      ]
    }
  ]
}
```

### 3.3 用户管理

#### 3.3.1 用户列表

**筛选条件：**

- 关键词搜索（ID、昵称、手机号）
- 注册时间范围
- 账号状态（正常、禁用）
- 用户等级

**列表字段：**

| 字段     | 显示内容                    |
| -------- | --------------------------- |
| 用户 ID  | 数字 ID                     |
| 头像     | 小图标                      |
| 昵称     | 文本                        |
| 手机号   | 脱敏显示（138\*\*\*\*1234） |
| 注册时间 | 日期时间                    |
| 最后登录 | 日期时间                    |
| 订单数   | 数字                        |
| 消费金额 | 金额                        |
| 状态     | 标签（正常/禁用）           |
| 操作     | 查看详情/禁用/启用          |

**批量操作：**

- 批量启用/禁用
- 批量导出 Excel

#### 3.3.2 用户详情

**Tab 页签：**

1. **基本信息**

   ```typescript
   interface UserInfo {
     id: string;
     avatar: string;
     nickname: string;
     realName?: string;
     phone: string;
     email?: string;
     gender?: "male" | "female";
     birthday?: string;
     signature?: string; // 个性签名
     registeredAt: string;
     lastLoginAt: string;
     status: "active" | "disabled";
   }
   ```

2. **宠物档案**

   - 宠物列表展示
   - 宠物详细信息

3. **订单记录**

   - 订单列表
   - 订单统计（总数、总额）

4. **社区动态**

   - 发布动态列表
   - 发帖数、获赞数统计

5. **操作记录**
   - 登录日志
   - 操作日志

### 3.4 商品管理

#### 3.4.1 商品列表

**顶部操作：**

- [+ 添加商品] 按钮
- [导入商品] 按钮
- [导出] 按钮

**筛选条件：**

```typescript
interface ProductFilter {
  keyword?: string; // 商品名称/ID
  categoryId?: string; // 分类
  brandId?: string; // 品牌
  priceRange?: [number, number]; // 价格区间
  status?: "on" | "off" | "out_of_stock"; // 状态
  createTimeRange?: [string, string]; // 创建时间
}
```

**列表显示：**

| 字段     | 宽度  | 说明             |
| -------- | ----- | ---------------- |
| ID       | 80px  | 商品 ID          |
| 图片     | 60px  | 主图缩略图       |
| 商品信息 | 300px | 名称+分类        |
| 价格     | 100px | 售价             |
| 库存     | 80px  | 总库存           |
| 销量     | 80px  | 总销量           |
| 状态     | 80px  | 上架/下架        |
| 创建时间 | 150px | 日期时间         |
| 操作     | 150px | 编辑/上下架/删除 |

**批量操作：**

- 批量上架/下架
- 批量修改价格
- 批量删除

#### 3.4.2 添加/编辑商品

**表单分步：**

**第一步：基本信息**

```typescript
interface ProductBasic {
  name: string; // 商品名称（必填）
  subtitle?: string; // 副标题
  categoryId: string; // 分类（必填）
  brandId: string; // 品牌（必填）
  mainImages: string[]; // 主图（1-5张，必填）
  detail: string; // 详情（富文本，必填）

  // 商品属性
  attributes: Array<{
    key: string; // 属性名
    value: string; // 属性值
  }>;

  // 适用信息
  petType: "dog" | "cat" | "other"; // 适用宠物
  ageRange?: string; // 适用年龄
  ingredients?: string; // 成分配料
}
```

**第二步：规格库存**

```typescript
interface ProductSKU {
  // 规格定义
  specs: Array<{
    name: string; // 规格名（如：颜色）
    values: string[]; // 规格值（如：红色、蓝色）
  }>;

  // SKU列表
  skus: Array<{
    specCombination: string; // 规格组合（如：红色-大号）
    skuCode: string; // SKU编码
    price: number; // 价格
    originalPrice: number; // 原价
    stock: number; // 库存
    weight?: number; // 重量（克）
  }>;
}
```

**第三步：销售信息**

```typescript
interface ProductSales {
  minBuyCount: number; // 起售数量
  maxBuyCount?: number; // 限购数量
  shippingTemplateId?: string; // 运费模板

  // SEO设置
  seoTitle?: string;
  seoKeywords?: string;
  seoDescription?: string;
}
```

**第四步：确认提交**

- 预览商品信息
- 设置上架状态
- 提交保存

#### 3.4.3 商品分类

**树形结构管理：**

```
┌─────────────────────────────────────┐
│  [+ 添加一级分类]                   │
├─────────────────────────────────────┤
│  📁 宠物食品                        │
│    ├─ 📁 犬粮              [编辑][删除]│
│    │   ├─ 幼犬粮          [编辑][删除]│
│    │   ├─ 成犬粮          [编辑][删除]│
│    │   └─ 老年犬粮        [编辑][删除]│
│    ├─ 📁 猫粮              [编辑][删除]│
│    │   ├─ 幼猫粮          [编辑][删除]│
│    │   └─ 成猫粮          [编辑][删除]│
│    └─ 📁 零食              [编辑][删除]│
│  📁 宠物用品                        │
│    ├─ 📁 清洁用品          [编辑][删除]│
│    └─ 📁 玩具              [编辑][删除]│
└─────────────────────────────────────┘
```

**分类字段：**

```typescript
interface Category {
  id: string;
  name: string; // 分类名称
  icon?: string; // 图标URL
  parentId?: string; // 父分类ID
  sort: number; // 排序
  status: "active" | "disabled"; // 状态
}
```

**操作功能：**

- 添加子分类
- 编辑分类
- 拖拽排序
- 启用/禁用
- 删除（需检查是否有商品）

### 3.5 订单管理

#### 3.5.1 订单列表

**Tab 分类：**

- 全部订单
- 待付款
- 待发货
- 已发货
- 已完成
- 已取消
- 退款/售后

**列表字段：**

| 字段     | 说明                       |
| -------- | -------------------------- |
| 订单号   | 可点击查看详情             |
| 下单时间 | 日期时间                   |
| 用户信息 | 昵称+手机号                |
| 商品信息 | 图片+名称+规格（折叠显示） |
| 订单金额 | 实付金额                   |
| 支付状态 | 未支付/已支付/已退款       |
| 订单状态 | 待发货/已发货等            |
| 操作     | 根据状态动态显示           |

**筛选搜索：**

```typescript
interface OrderFilter {
  keyword?: string; // 订单号/用户昵称/手机号
  status?: OrderStatus; // 订单状态
  paymentMethod?: "online" | "cod"; // 支付方式
  createTimeRange?: [string, string]; // 下单时间
  amountRange?: [number, number]; // 金额区间
}
```

#### 3.5.2 订单详情

**页面布局：**

```
┌─────────────────────────────────────────────┐
│  订单号: 2024011512345678       [打印订单]  │
├─────────────────────────────────────────────┤
│  订单状态流程                                │
│  [下单] → [支付] → [发货] → [完成]          │
│   ✓       ✓        当前      待完成          │
├─────────────────────────────────────────────┤
│  用户信息                                    │
│  昵称: 张三  手机: 138****1234              │
├─────────────────────────────────────────────┤
│  收货信息                       [修改地址]  │
│  收货人: 张三  电话: 138****1234            │
│  地址: 北京市朝阳区xxx街道xxx号              │
├─────────────────────────────────────────────┤
│  商品清单                                    │
│  [图] 商品名称(规格)  ￥299 × 1 = ￥299     │
│  [图] 商品名称(规格)  ￥199 × 2 = ￥398     │
├─────────────────────────────────────────────┤
│  价格明细                                    │
│  商品小计: ￥697                             │
│  运费: ￥10                                  │
│  优惠券: -￥20                               │
│  实付金额: ￥687                             │
├─────────────────────────────────────────────┤
│  物流信息                       [修改物流]  │
│  物流公司: 顺丰速运                          │
│  运单号: SF1234567890                       │
│  发货时间: 2024-01-15 14:30                 │
├─────────────────────────────────────────────┤
│  订单日志                                    │
│  2024-01-15 14:30 商家已发货                │
│  2024-01-15 10:25 用户已支付                │
│  2024-01-15 10:20 用户下单                  │
├─────────────────────────────────────────────┤
│  操作区                                      │
│  [备注订单] [修改价格] [发货] [取消订单]    │
└─────────────────────────────────────────────┘
```

**发货操作：**

```typescript
interface ShippingForm {
  expressCompany: string; // 物流公司
  trackingNumber: string; // 运单号
  remark?: string; // 备注
}
```

#### 3.5.3 售后管理

**售后类型：**

- 仅退款
- 退货退款
- 换货

**售后流程：**

```
用户申请 → 商家审核 → 用户退货 → 商家收货 → 退款完成
                ↓
              拒绝申请
```

**售后列表：**

| 字段     | 说明                   |
| -------- | ---------------------- |
| 售后单号 | 唯一编号               |
| 订单号   | 关联订单               |
| 申请时间 | 日期时间               |
| 用户昵称 | 申请人                 |
| 售后类型 | 仅退款/退货退款/换货   |
| 退款金额 | 金额                   |
| 售后原因 | 文本                   |
| 状态     | 待审核/已同意/已拒绝等 |
| 操作     | 查看/同意/拒绝         |

### 3.6 社区管理

#### 3.6.1 动态管理

**列表显示：**

| 字段     | 说明                     |
| -------- | ------------------------ |
| 动态 ID  | 数字 ID                  |
| 用户昵称 | 发布人                   |
| 动态内容 | 文本缩略（50 字符）      |
| 媒体预览 | 图片/视频缩略图          |
| 发布时间 | 日期时间                 |
| 点赞数   | 数字                     |
| 评论数   | 数字                     |
| 状态     | 正常/已隐藏/已删除       |
| 操作     | 查看/置顶/加精/隐藏/删除 |

**批量操作：**

- 批量删除
- 批量隐藏

#### 3.6.2 评论管理

**列表显示：**

| 字段     | 说明               |
| -------- | ------------------ |
| 评论 ID  | 数字 ID            |
| 用户昵称 | 评论人             |
| 评论内容 | 文本               |
| 关联动态 | 动态标题（可点击） |
| 评论时间 | 日期时间           |
| 状态     | 正常/已删除        |
| 操作     | 查看/删除          |

#### 3.6.3 话题管理

**列表显示：**

| 字段     | 说明           |
| -------- | -------------- |
| 话题名称 | 如：#萌宠日常  |
| 话题描述 | 文本说明       |
| 封面图   | 图片           |
| 帖子数   | 统计数字       |
| 参与人数 | 统计数字       |
| 创建时间 | 日期时间       |
| 是否推荐 | 是/否          |
| 状态     | 启用/禁用      |
| 操作     | 编辑/推荐/删除 |

**话题表单：**

```typescript
interface TopicForm {
  name: string; // 话题名称（必填）
  description: string; // 话题描述
  cover: string; // 封面图URL
  isRecommended: boolean; // 是否推荐
  status: "active" | "disabled"; // 状态
}
```

#### 3.6.4 举报处理

**举报列表：**

| 字段       | 说明                 |
| ---------- | -------------------- |
| 举报 ID    | 数字 ID              |
| 举报类型   | 动态/评论/用户       |
| 被举报对象 | 对象信息             |
| 举报原因   | 文本                 |
| 举报人     | 用户昵称             |
| 举报时间   | 日期时间             |
| 处理状态   | 待处理/已处理/已忽略 |
| 操作       | 查看/处理            |

**处理操作：**

- 查看详情（举报内容）
- 删除被举报内容
- 警告用户
- 封禁用户（1 天/7 天/永久）
- 标记已处理
- 忽略举报

### 3.7 内容管理

#### 3.7.1 文章管理

**列表显示：**

| 字段     | 说明           |
| -------- | -------------- |
| 文章 ID  | 数字 ID        |
| 文章标题 | 可点击预览     |
| 分类     | 分类名称       |
| 作者     | 作者姓名       |
| 浏览量   | 统计数字       |
| 发布状态 | 草稿/已发布    |
| 发布时间 | 日期时间       |
| 操作     | 编辑/删除/预览 |

**文章表单：**

```typescript
interface ArticleForm {
  title: string; // 文章标题（必填）
  categoryId: string; // 分类（必填）
  cover: string; // 封面图（必填）
  summary: string; // 摘要
  content: string; // 内容（富文本，必填）
  authorId: string; // 作者
  tags: string[]; // 标签
  seoTitle?: string; // SEO标题
  seoKeywords?: string; // SEO关键词
  seoDescription?: string; // SEO描述
  publishStatus: "draft" | "published"; // 发布状态
  publishTime?: string; // 定时发布时间
}
```

#### 3.7.2 Banner 管理

**列表显示：**

```
┌─────────────────────────────────────────────────────┐
│  [+ 添加Banner]                                     │
├─────────────────────────────────────────────────────┤
│  [拖动调整排序]                                      │
│  ┌───────┐  标题: 新年促销活动                      │
│  │       │  位置: 首页顶部                           │
│  │ 预览图 │  链接: /activity/newyear                │
│  │       │  时间: 2024-01-01 ~ 2024-01-31           │
│  └───────┘  状态: 启用  [编辑] [禁用] [删除]        │
├─────────────────────────────────────────────────────┤
│  ┌───────┐  标题: 新品上市                          │
│  │       │  位置: 首页顶部                           │
│  │ 预览图 │  链接: /product/new                     │
│  │       │  时间: 长期有效                           │
│  └───────┘  状态: 启用  [编辑] [禁用] [删除]        │
└─────────────────────────────────────────────────────┘
```

**Banner 表单：**

```typescript
interface BannerForm {
  title: string; // 标题
  image: string; // 图片URL（必填）
  link: string; // 跳转链接
  position: "home" | "shop" | "community"; // 展示位置
  sort: number; // 排序
  startTime?: string; // 开始时间
  endTime?: string; // 结束时间
  status: "active" | "disabled"; // 状态
}
```

### 3.8 营销管理

#### 3.8.1 优惠券管理

**优惠券类型：**

| 类型   | 说明           | 示例         |
| ------ | -------------- | ------------ |
| 满减券 | 满 X 元减 Y 元 | 满 200 减 30 |
| 折扣券 | X 折优惠       | 8 折券       |
| 兑换券 | 直接抵扣       | 50 元券      |
| 包邮券 | 免运费         | 全场包邮     |

**优惠券表单：**

```typescript
interface CouponForm {
  name: string; // 优惠券名称
  type: "full_reduce" | "discount" | "exchange" | "shipping"; // 类型
  amount: number; // 面额/折扣
  threshold: number; // 使用门槛（满X元）
  totalCount: number; // 发放总数
  perUserLimit: number; // 每人限领数量
  startTime: string; // 有效期开始
  endTime: string; // 有效期结束

  // 使用范围
  scope: "all" | "category" | "product"; // 全场/分类/指定商品
  scopeIds?: string[]; // 分类ID或商品ID数组

  // 发放方式
  distributeType: "manual" | "auto" | "claim"; // 手动/自动/用户领取
}
```

**优惠券列表：**

| 字段          | 说明                 |
| ------------- | -------------------- |
| 优惠券名称    | 文本                 |
| 类型          | 满减/折扣/兑换/包邮  |
| 面额          | 金额/折扣            |
| 使用门槛      | 满 X 元可用          |
| 发放量/已使用 | 数量统计             |
| 有效期        | 日期范围             |
| 状态          | 未开始/进行中/已结束 |
| 操作          | 编辑/删除/发放       |

### 3.9 数据统计

#### 3.9.1 运营概览

**Dashboard 布局：**

```
┌─────────────────────────────────────────────────────┐
│  数据概览（今日）                                    │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────┐│
│  │ 销售额    │ │ 订单数    │ │ 新增用户  │ │ 访问量││
│  │ ￥12,345 │ │   156    │ │   234    │ │ 5,678 ││
│  │ ↑ 12.5%  │ │ ↑ 8.3%   │ │ ↓ 3.2%   │ │ ↑ 15% ││
│  └───────────┘ └───────────┘ └───────────┘ └───────┘│
├─────────────────────────────────────────────────────┤
│  销售额趋势图（最近7天）                             │
│  ┌─────────────────────────────────────────────┐   │
│  │              📈 ECharts折线图                │   │
│  └─────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────┤
│  订单量趋势图（最近30天）                            │
│  ┌─────────────────────────────────────────────┐   │
│  │              📊 ECharts柱状图                │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

**核心指标：**

```typescript
interface DashboardMetrics {
  // 今日数据
  today: {
    sales: number; // 销售额
    orders: number; // 订单数
    newUsers: number; // 新增用户
    pageViews: number; // 访问量
  };

  // 对比昨日涨跌
  comparison: {
    salesChange: number; // 销售额变化率
    ordersChange: number; // 订单数变化率
    usersChange: number; // 用户变化率
    viewsChange: number; // 访问量变化率
  };

  // 趋势数据
  trends: {
    sales: Array<{ date: string; value: number }>;
    orders: Array<{ date: string; value: number }>;
  };
}
```

#### 3.9.2 用户分析

**用户概况：**

| 指标     | 今日  | 本周  | 本月   |
| -------- | ----- | ----- | ------ |
| 总用户数 | -     | -     | 50,000 |
| 新增用户 | 234   | 1,680 | 7,200  |
| 活跃用户 | 2,156 | 8,900 | 25,000 |
| 留存率   | -     | 68%   | 52%    |

**用户画像图表：**

1. **性别分布（饼图）**

   - 男性：35%
   - 女性：65%

2. **年龄分布（柱状图）**

   - 18-25 岁：15%
   - 26-30 岁：35%
   - 31-40 岁：33%
   - 41-50 岁：12%
   - 50 岁以上：5%

3. **地域分布（地图）**

   - TOP10 城市用户量

4. **设备分布（饼图）**
   - 移动端：65%
   - PC 端：30%
   - 平板：5%

#### 3.9.3 商品分析

**销售排行：**

**商品销售额 TOP20：**

| 排名 | 商品名称            | 分类 | 销售额    | 销量 | 客单价 |
| ---- | ------------------- | ---- | --------- | ---- | ------ |
| 1    | 皇家小型成犬粮 8kg  | 犬粮 | ￥ 28,912 | 97   | ￥ 298 |
| 2    | 冠能大型成犬粮 15kg | 犬粮 | ￥ 25,680 | 64   | ￥ 401 |
| ...  | ...                 | ...  | ...       | ...  | ...    |

**分类销售占比（饼图）：**

- 宠物食品：60%
- 宠物用品：25%
- 宠物玩具：10%
- 其他：5%

**库存预警：**

| 商品名称 | 当前库存 | 预警线 | 状态        |
| -------- | -------- | ------ | ----------- |
| 商品 A   | 5        | 10     | 🔴 低库存   |
| 商品 B   | 12       | 20     | 🟡 库存偏低 |

#### 3.9.4 财务报表

**日报表：**

| 日期       | 营业收入  | 退款金额 | 实际收入  | 订单数 | 客单价  |
| ---------- | --------- | -------- | --------- | ------ | ------- |
| 2024-01-15 | ￥ 12,345 | ￥ 567   | ￥ 11,778 | 156    | ￥ 75.5 |
| 2024-01-14 | ￥ 10,980 | ￥ 432   | ￥ 10,548 | 142    | ￥ 74.3 |

**导出功能：**

- 导出 Excel
- 导出 PDF
- 自定义日期范围

### 3.10 技术实现要点

#### 3.10.1 权限控制

**路由守卫：**

```typescript
// router/index.ts
router.beforeEach((to, from, next) => {
  const userStore = useUserStore();

  // 检查登录
  if (!userStore.isLoggedIn && to.path !== "/login") {
    return next("/login");
  }

  // 检查权限
  const requiredPermission = to.meta.permission;
  if (requiredPermission && !userStore.hasPermission(requiredPermission)) {
    return next("/403");
  }

  next();
});
```

**按钮级权限：**

```vue
<template>
  <el-button v-permission="'product:delete'" @click="handleDelete">
    删除
  </el-button>
</template>

<script setup lang="ts">
// directives/permission.ts
const vPermission = {
  mounted(el: HTMLElement, binding: any) {
    const userStore = useUserStore();
    const permission = binding.value;

    if (!userStore.hasPermission(permission)) {
      el.remove();
    }
  },
};
</script>
```

#### 3.10.2 数据导出

```typescript
// utils/export.ts
import ExcelJS from "exceljs";

export async function exportToExcel(
  data: any[],
  columns: any[],
  filename: string
) {
  const workbook = new ExcelJS.Workbook();
  const worksheet = workbook.addWorksheet("Sheet1");

  // 设置列
  worksheet.columns = columns.map((col) => ({
    header: col.label,
    key: col.prop,
    width: col.width || 15,
  }));

  // 添加数据
  worksheet.addRows(data);

  // 样式设置
  worksheet.getRow(1).font = { bold: true };
  worksheet.getRow(1).alignment = { vertical: "middle", horizontal: "center" };

  // 生成文件
  const buffer = await workbook.xlsx.writeBuffer();
  const blob = new Blob([buffer], {
    type: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
  });

  // 下载
  const link = document.createElement("a");
  link.href = URL.createObjectURL(blob);
  link.download = `${filename}.xlsx`;
  link.click();
}
```

#### 3.10.3 图表组件

```vue
<template>
  <div ref="chartRef" style="width: 100%; height: 400px;"></div>
</template>

<script setup lang="ts">
import { ref, onMounted } from "vue";
import * as echarts from "echarts";

const chartRef = ref<HTMLElement>();

onMounted(() => {
  const chart = echarts.init(chartRef.value!);

  chart.setOption({
    title: { text: "销售额趋势" },
    tooltip: { trigger: "axis" },
    xAxis: {
      type: "category",
      data: ["1/10", "1/11", "1/12", "1/13", "1/14", "1/15", "1/16"],
    },
    yAxis: { type: "value" },
    series: [
      {
        name: "销售额",
        type: "line",
        data: [8200, 9100, 8500, 10200, 9800, 11500, 12300],
        smooth: true,
      },
    ],
  });
});
</script>
```
